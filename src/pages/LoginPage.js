import React from 'react';
import Form from "../components/Form";

const LoginPage = () => {
    return (
        <div>
            <Form  page={'Login'}/>
        </div>
    );
};

export default LoginPage;